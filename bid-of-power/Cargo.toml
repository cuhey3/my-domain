[package]
name = "bid-of-power"
version = "0.1.0"
edition = "2024"

[dependencies]
board-games = {path= "../board-games" }
# wasm対応
rand = { version = "0.9.2", default-features = false, features = ["small_rng"] }
