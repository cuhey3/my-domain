[package]
name = "board-games"
version = "0.1.0"
edition = "2024"

[dependencies]
# wasm対応
rand = { version = "0.9.2", default-features = false, features = ["small_rng"] }
board-game-if = { git = "https://github.com/cuhey3/my-if" }